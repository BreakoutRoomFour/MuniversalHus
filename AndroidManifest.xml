//YOU SHOULD NOT CREATE THIS AS AN XML FILE
//FOLLOW THE GUIDLINES BELOW
//Note: You can only go this file through a different path
//Here's what ya do...
//1. Go to Project > app > src > main > AndroidManifest.xml
//2. Copy/paste the code given below

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalApp">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MuQuestions">
        </activity>
    </application>

</manifest>
